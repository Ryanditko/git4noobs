// Make a new branch and switch to it
const git_checkout = 'git checkout -b new-branch-name'
// Make a new branch and switch to it
const git_checkout_to_main = 'git checkout main'
// Make a new branch and switch to it
const git_checkout_to_new_branch = 'git checkout new-branch-name'
// Make a new branch and switch to it
const git_checkout_to_new_branch_and_make_a_commit = 'git checkout new-branch-name && git commit -m "first commit"'
// Make a new branch and switch to it
const git_checkout_to_new_branch_and_make_a_commit_and_push = 'git checkout new-branch-name && git commit -m "first commit" && git push -u origin'